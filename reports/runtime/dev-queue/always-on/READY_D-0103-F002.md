# Backlog item — D-0103-F002 (tts-engine-contracts)

```yaml
schema: backlog-item-v1
id: D-0103-F002
title: tts-engine-contracts
created_at: 2026-09-19T23:10:04.272Z
created_by: gpt-web
repository: mrhz1973/tmar-tts
branch_target: main

objective: |
  Implement generic TTS request/result, capabilities and load/unload lifecycle per the F001 architecture contract (docs/architecture.md): create tmar/core/contracts.py defining TTSEngine, TTSRequest, TTSResult and Capability exactly as contracted, plus a focused mock-based test suite. No second engine and no GUI migration in the same delta.

scope:
  allowed_areas:
    - tmar/core/contracts.py
    - tmar/core/__init__.py
    - tests/test_engine_contracts.py
    - docs/current-state.md
    - docs/decisions.md
  forbidden_areas:
    - *.py
    - requirements.txt
    - requirements-dev.txt
    - ruff.toml
risk_hint: low
complexity_hint: medium

planner:
  preferred: qwen
  fallback: []
  fallback_policy: gate_only

execution:
  target: cursor
  loop_allowed: true
  max_loop_rounds_hint: 2

acceptance:
  - tmar/core/contracts.py defines TTSEngine protocol, TTSRequest, TTSResult, Capability per F001 contract
  - focused mock test suite tests/test_engine_contracts.py passes
  - no GUI change, no second engine, no pipeline change
  - docs/current-state.md updated for the new contracts module
  - git diff --check passes

local_dev:
  dev_profile: qwen38-opus-q3-opencode-64k
  timebox_hint: 3600
  max_turns_hint: 16
  test_commands:
    - powershell.exe -NoProfile -Command "if (-not (Test-Path tmar\core\contracts.py)) { exit 1 }; if (-not (Test-Path tests\test_engine_contracts.py)) { exit 2 }; .venv\Scripts\python.exe tests\test_engine_contracts.py; if ($LASTEXITCODE -ne 0) { exit 3 }; git diff --check; if ($LASTEXITCODE -ne 0) { exit 4 }"

human_gate_required_if: []
context_refs:
    - github:mrhz1973/control-plane#103
    - github:mrhz1973/control-plane#110

state: READY_FOR_PLANNING
```
