# V4 WF90 post-cutover Telegram SERVICE_ERROR storm remediation

## Classification

- `TASK_REF=V4_WF90_POST_CUTOVER_TELEGRAM_SERVICE_ERROR_STORM_REMEDIATION_V1`
- `BASE_HEAD=3486f028ee6a347339956e4e892c0a4d6ecf669d`
- `ISSUE=#72`
- `WF90_POST_CUTOVER_TELEGRAM_STORM_FIXED=PASS`
- `ROOT_CAUSE=NEW_N8N_TO_TAILSCALE_ROUTE_FAILURE`
- `ROOT_CAUSE_DETAIL=TAILSCALE_POLICY_MISSING_NEW_TO_ASUSDESKTOP_TCP443_GRANT`
- `TWO_NATURAL_TICKS=PASS`
- `SPURIOUS_TELEGRAM=0`
- `NEW_LIVE=HEALTHY`
- `OLD_CHANGED=NO`
- `SECRET_VALUES_EXPOSED=0`

## Mandatory diagnosis before mutation

Git preflight passed on `main`: local HEAD, `origin/main`, and remote main were
all the expected base. There was no tracked dirty state.

LIVE WF90 on NEW was the intended workflow:

- ID `90ldaa5a-4000-8000-000000000090`
- name `90 - CP V4 LOCAL DEV ALWAYS-ON DISPATCHER - ACTIVE`
- active/published `true`
- `activeVersionId=versionId=febca537-9218-4fb4-8280-847b5e961f6b`
- HTTP URL
  `https://asusdesktop.tailc01234.ts.net/v4/local-dev/dispatch-tick`
- HTTP node `onError=continueRegularOutput`
- normalizer jsCode SHA-256
  `9f4184e9802ea4a08779977eb4c02bfbc27b330e16b4c19ea07c7be75e2c39ec`,
  2029 UTF-8 bytes: byte-equivalent to the canonical repository artifact
- Telegram credential binding type remained `telegramApi`; no value was read
  or persisted

Execution `313883` (`2026-09-07T18:40:41.041Z` to
`2026-09-07T18:42:55.470Z`) captured the real failure:

- HTTP Request regular output shape: top-level key `error`
- sanitized error: `connect ETIMEDOUT 100.110.35.23:443`, code `ETIMEDOUT`
- normalizer output:
  `response_valid=false`, `classification=SERVICE_ERROR`,
  `execution_performed=false`, `task_ref=null`,
  `human_gate_required=false`, `notify_required=true`
- Telegram node run count: `1`

`isTickResult()` was false for the correct reason: its input was an Axios
connect-error envelope containing no recoverable
`local-dev-dispatch-tick-result-v1`, rather than a dispatcher result. This was
not stale normalizer behavior.

## Boundary isolation

- NEW host and n8n container both resolved
  `asusdesktop.tailc01234.ts.net` to `100.110.35.23`.
- NEW Tailscale ping reached `asusdesktop`.
- Before the fix, NEW host TCP/443, `tailscale nc`, host HTTPS, and n8n
  container HTTPS all timed out.
- Windows Tailscale was connected with `ShieldsUp=false`.
- Tailscale Serve was private (`tailnet only`) and mapped exactly
  `/v4/local-dev/dispatch-tick` to `127.0.0.1:18793/v1/tick`; Funnel was not
  used.
- Scheduled Task `ControlPlane-V4-LocalDevDispatcher` was enabled with last
  result `0`. Its detached Node process was alive, and exactly one listener
  existed on `127.0.0.1:18793`.
- A local schema-valid request returned HTTP 200 with
  `local-dev-dispatch-tick-result-v1`, `IDLE_CLEAN`, and
  `execution_performed=false`.

The existing Tailscale rules explained the cutover-specific asymmetry: OLD
`100.114.7.53` already had a TCP/443 grant to workstation
`100.110.35.23`; NEW `100.99.54.93` did not. Dispatcher, Serve, DNS, and WF90
were healthy. The missing source grant was therefore the proven root cause.

## Minimum correction

One private Tailscale grant was added:

```text
src = 100.99.54.93/32
dst = 100.110.35.23/32
ip  = tcp:443
note = WF90 NEW production dispatcher path after VPS cutover (#72)
```

The pre-existing OLD rollback grant and GOI member grants were preserved. No
ACL broadening to `autogroup:member`, no public listener, Funnel, DNS change,
workflow mutation, dispatcher restart, or OLD mutation occurred.

After propagation, NEW TCP/443 succeeded and a request from the effective n8n
container returned HTTP 200 with a valid
`local-dev-dispatch-tick-result-v1`, `IDLE_CLEAN`, and
`execution_performed=false`.

## Focused and natural acceptance

The canonical normalizer suite passed `11/11`. It proves:

- `IDLE_CLEAN`, `BUSY`, and healthy non-notifiable results remain silent;
- `HUMAN_GATE_REQUIRED` remains `notify_required=true`;
- `WORK_EXECUTED_STOP` remains `notify_required=true`;
- malformed/new real service errors remain fail-closed `SERVICE_ERROR` with
  notification enabled.

The first two completed natural schedule executions strictly after the
post-fix proof timestamp `2026-09-07T18:52:42Z` were:

| execution | started UTC | stopped UTC | status | classification | response_valid | notify_required | Telegram runs |
|---|---|---|---|---|---|---|---|
| `313916` | `18:55:41.032` | `18:55:42.680` | success | IDLE_CLEAN | true | false | 0 |
| `313927` | `19:00:41.059` | `19:00:42.439` | success | IDLE_CLEAN | true | false | 0 |

Both HTTP outputs had the expected tick-result schema and no HTTP error.
Therefore `TWO_NATURAL_TICKS=PASS` and no spurious Telegram message was
generated by either execution.

## Preserved production state

- NEW n8n health: HTTP 200; PostgreSQL healthy.
- Exact publication map remained four workflows; WF90 and WF40 versions were
  unchanged.
- WF40 remained active at
  `a609ad90-7eb4-4495-9ec5-c4413165cea1` with recent successful natural
  executions.
- WF61 remained inactive with no active version; `D0025_ENABLED=false`
  remained canonical.
- NEW Tailscale identity remained
  `ionos-n8n-new.tailc01234.ts.net` / `100.99.54.93`; Serve and Funnel on NEW
  remained absent.
- OLD n8n remained exited/frozen; OLD PostgreSQL remained healthy at the
  frozen `10176` executions / max ID `312840`.
- `CUTOVER=PASS`, `NEW_ROLE=LIVE`,
  `OLD_ROLE=ROLLBACK_STANDBY_FROZEN`,
  `ROLLBACK_RETENTION=OPEN`,
  `OLD_DECOMMISSION_AUTHORIZED=NO`.

## Final result

`PASS — WF90_POST_CUTOVER_TELEGRAM_STORM_FIXED ·
ROOT_CAUSE=NEW_N8N_TO_TAILSCALE_ROUTE_FAILURE · TWO_NATURAL_TICKS=PASS ·
SPURIOUS_TELEGRAM=0 · NEW_LIVE=HEALTHY · OLD_CHANGED=NO`
