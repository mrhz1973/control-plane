# D-0014-W — Current gate

No human gate is required for the next **read-only planner/discovery** step.

A real human gate is required before any of the following:
- starting/installing OpenClaw gateway/service/daemon on Windows;
- changing Tailscale, ACLs, routes, firewall, port exposure or reverse proxy;
- modifying n8n workflows/runtime wiring;
- mutating credentials/auth/billing/config;
- issuing new provider/model requests;
- promoting Windows from fallback to canonical primary.
