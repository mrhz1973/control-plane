# AUTONOMOUS SPRINT 2026-09-05

Status: READY FOR ONE BOUNDED CURSOR CAMPAIGN

Parent: #32. First corrective dependency: #38.

## Goal

Use one unattended Cursor Agent session for up to ~90 minutes to advance the quota-aware routing foundation without Codex/Qwen inference for qualification work and without live n8n/runtime authorization changes.

## Sequential phases

1. Reconcile #38: Codex IDE static registry qualification metadata must match completed #34 while live quota collector remains missing.
2. Add an offline/manual Codex dashboard-snapshot translator that can normalize explicitly supplied Usage-dashboard observations into the existing RESOURCE_STATUS contribution shape; no scraping/browser automation and no hardcoded quota numbers.
3. Add an offline/manual GLM Coding Plan snapshot translator that normalizes explicitly supplied shared-pool observations for `glm-5.3` + `glm-5.3-flash`; no invented live quota/reset values.
4. Add offline quota-pool-aware selection/evaluation tests/policy consumption using existing registry-v2 relationships, fail-closed on stale/missing pool status; no live route activation.

## Campaign law

- Execute phases strictly in order.
- Each phase gets its own focused evidence and commit.
- Before each phase, fetch/verify `origin/main` equals local HEAD.
- Proceed to the next phase only after predecessor focused tests PASS and commit/push/remote verify PASS.
- First STOP/failure ends the entire campaign; do not continue to later phases.
- Never reset/rebase/force-push/stash/clean.
- No OpenAI API/BYOK; no Codex qualification generation; no Qwen generation for quota discovery; no workflow/n8n/WF40/WF61/D-0025/production changes.
- Historical evidence is not rewritten.
- One final campaign report summarizes commits, tests, and first unresolved gate.

This document is a campaign plan; execution still occurs only through the explicit Cursor Agent prompt supplied by GPT Web.
