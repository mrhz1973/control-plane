# LAST CURSOR REPORT

**BLOCK-ID:** `V4_LOCAL_DEV_EXECUTOR_OPENCODE_PERMISSION_SCHEMA_FIX_V1`
**Classification:** `OPENCODE_PERMISSION_SCHEMA_FIXED`
**Timestamp (local):** 2026-09-04

## Summary

Fixed the exact Retry3 OpenCode V1 schema blocker: removed the undocumented
`permission._network_policy` metadata field. The envelope network policy
remains executor metadata only; no replacement undocumented key was added.

The exact live config generated by `buildPermissionOverlay` +
`buildOpenCodeRuntimeConfig` is now validated through installed OpenCode V1
`debug config` for BOTH `localhost_only` and `offline`, without mutation.
Technical enforcement remains: webfetch/websearch deny, bash deny-all-first
allowlist, edit scope, DEV guard-only model traffic, no direct :8080.

Tests: wiring **32/32**, executor **20/20**, workstation bridge **14/14**.
No live proof, Qwen generation, OpenCode model execution, or service lifecycle
was performed.

## NEXT

`V4_LOCAL_DEV_EXECUTOR_QWEN_FIRST_BOUNDED_LIVE_PROOF_RETRY4`

Evidence report:
`reports/architecture/v4_local_dev_executor_opencode_permission_schema_fix_v1.md`
